@using PagedList;
@using PagedList.Mvc;
@model IPagedList<SchoolWebApp.Models.TimeTables>

@{
    ViewBag.Title = "الجداول";
    Layout = "~/Views/Shared/_MainLayout.cshtml";
}

<div class="thumbnail" style="padding-bottom:100px;padding-right:0px ;">
    <div style="background:url('../../Content/images/about_schools.jpg') center ;height:200px;">
    </div>

    <div class="col-md-12" style="background-color:lavender; margin-top:5px;padding-top:5px; height:50px;">

        <h2 style="text-align:center">
            <span class="glyphicon glyphicon-file"></span>
             الجداول   &nbsp;&nbsp;
        </h2>

    </div>



    <div class="container">
        <div class="row">

            <div class="col-md-3 thumbnail">
                <p class="btn btn-block btn-success glyphicon glyphicon-plus" style="height:30px;font-size:medium">
                    @Html.ActionLink("  إضافة  صنف جديد  ", "Create", null, new { @style = " color:black;font-weight:500;font-size:larger" })
                </p>


            </div>
            <div class="table-responsive col-md-12" style="padding:0px">
                <table class="table table-striped table-hover">
                    <tr style="background-color:lavenderblush">
                        <th>
                            @Html.DisplayNameFor(model => model.First().TimeTablesType.TimeTablesTypeName)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.First().TableTitle)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.First().TableTitleDetails)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.First().TableTitleURL)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.First().TableTitleDate)
                        </th>
                        <th></th>
                    </tr>

                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.TimeTablesType.TimeTablesTypeName)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.TableTitle)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.TableTitleDetails)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.TableTitleURL)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.TableTitleDate).ToString().Substring(0,10)
                            </td>
                            <td>


                                @Html.ActionLink(" ", "Edit", new { id = item.Id }, new { @class = "glyphicon glyphicon-edit", title = "تعديل" }) &nbsp; | &nbsp;
                                @Html.ActionLink(" ", "Details", new { id = item.Id }, new { @class = "glyphicon glyphicon-eye-open", title = "تفاصيل" }) &nbsp; | &nbsp;
                                @Html.ActionLink(" ", "Delete", new { id = item.Id }, new { @class = "glyphicon glyphicon-trash", title = "حذف" })&nbsp;
                            </td>
                        </tr>
                    }

                </table>
                @Html.PagedListPager(Model, Page => Url.Action("Index", new { Page }))

            </div>
            <p class="col-md-2 btn-lg btn-primary" role="button">

                <span class="glyphicon glyphicon-share-alt"></span>
                @Html.ActionLink("العودة إلى القائمة", "../home/index", null, new { @style = "color:black;font-weight:500" })


            </p>
        </div>
    </div>
</div>
